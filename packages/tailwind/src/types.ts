/**
 * Represents a CSS style object where both keys and values are strings
 * Example:
 * {
 *   "color": "red",
 *   "background-color": "blue",
 *   "font-size": "16px"
 * }
 */
export interface StyleObject {
  [key: string]: string
}
